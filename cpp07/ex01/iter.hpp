#pragma once

#include <string>
#include <iostream>
#include <cmath>

template <typename T, typename U, typename F>
void	iter(T *array, U l, F enf)
{
	for (U i = 0; i < len; i++)
	{
		f(array[i]);
	}
}
